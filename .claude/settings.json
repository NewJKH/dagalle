{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "timeout": 60,
            "statusMessage": "변경사항 커밋 & 푸시 중...",
            "command": "cd \"/Users/jang-gunho/Library/Mobile Documents/com~apple~CloudDocs/Desktop/Spring/dagalle\" || exit 0; BRANCH=$(git branch --show-current); if [ -z \"$BRANCH\" ] || [ \"$BRANCH\" = \"main\" ]; then echo \"[skip] main 또는 detached HEAD — 자동 커밋하지 않음\"; exit 0; fi; git add -- src frontend/src frontend/package.json docs build.gradle settings.gradle CLAUDE.md .gitignore 2>/dev/null; if git diff --cached --quiet; then exit 0; fi; MSG=$(git diff --cached --name-only | head -5 | tr '\\n' ', ' | sed 's/,$//'); git commit -m \"auto: ${MSG}\" && git push origin \"$BRANCH\" 2>&1"
          }
        ]
      }
    ]
  }
}
